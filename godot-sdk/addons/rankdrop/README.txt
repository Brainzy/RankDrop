# RankDrop SDK for Godot 4

How to use:
1. Copy the `addons/rankdrop` folder into your Godot project.
2. Enable the plugin in Project Settings > Plugins.
3. Call `RankDrop.configure(server_url, game_secret, default_leaderboard)` before making requests.
